(function(){'use strict';
const SUPA_URL='https://fyvogvjvivxpqztbaoct.supabase.co';
const SUPA_KEY='sb_publishable_gnvNDN1co5qB7tBBevXziQ_pbAoBc87';
const sb=window.supabase.createClient(SUPA_URL,SUPA_KEY);
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const esc=s=>String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const n=v=>Number(v)||0, norm=s=>String(s||'').trim().toLowerCase();
const today=()=>{const d=new Date();d.setHours(0,0,0,0);return d};
const dparse=s=>{if(!s)return null;const d=new Date(String(s).slice(0,10)+'T00:00:00');return isNaN(d)?null:d};
const iso=d=>d?`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`:'';
const addDays=(s,x)=>{const d=dparse(s)||today();d.setDate(d.getDate()+x);return iso(d)};
const diffDays=(a,b)=>{const A=dparse(a),B=dparse(b);if(!A||!B)return 0;return Math.floor((B-A)/86400000)};
const fmt=v=>Math.round(n(v)).toLocaleString('en-PH');
const CLIENT_STAGES=[['New Client / Requirements',10],['Logo / Mockup',20],['Awaiting Client Approval',30],['Finalizing Artwork',40],['Waiting for Down Payment',50],['For Production',60],['On Production',75],['Quality Check',85],['Ready for Release',92],['Delivered',97],['Completed',100]];
const SUPPLIER_STAGES=[['Not Sent to Supplier',0],['Sent to Supplier',10],['Artwork Check',20],['Plate / Screen Setup',35],['Printing',60],['Quality Check',80],['In Transit',90],['Received',100],['Completed',100]];
const stagePct=name=>{const x=CLIENT_STAGES.find(a=>a[0]===name);return x?x[1]:0};
const supplierPct=name=>{const x=SUPPLIER_STAGES.find(a=>a[0]===name);return x?x[1]:0};
let DATA={quotations:[],products:[],workflows:new Map()}, PROJECTS=[], CURRENT=null;
function qClient(q){return String(q.company||q.customer||q.contact||'Unassigned Client').trim()||'Unassigned Client'}
function qDate(q){return String(q.date||q.createdAt||'').slice(0,10)}
function defaultStage(q){const s=String(q.orderStatus||'');if(s==='Completed')return 'Completed';if(s==='Delivered')return 'Delivered';if(s==='Ready for Release')return 'Ready for Release';if(s==='Ongoing Production')return 'On Production';if(s==='For Production')return 'For Production';if(s==='Waiting for Down Payment')return 'Waiting for Down Payment';if(s==='For Confirmation')return 'Logo / Mockup';return q.quotationStatus==='Approved'?'Finalizing Artwork':'New Client / Requirements'}
function defaultNext(stage){const m={'New Client / Requirements':'Collect logo, cup size, print details and quantity','Logo / Mockup':'Prepare / revise logo and cup mockup','Awaiting Client Approval':'Follow up client approval','Finalizing Artwork':'Lock final artwork for production','Waiting for Down Payment':'Follow up required payment','For Production':'Send final job order to supplier / production','On Production':'Request supplier production update','Quality Check':'Check print quality and quantity','Ready for Release':'Arrange release / delivery','Delivered':'Confirm receipt and payment','Completed':'No pending action'};return m[stage]||'Review next action'}
function supplierRisk(issue,stage){if(['Received','Completed'].includes(stage))return {key:'good',label:'Done',score:9,days:diffDays(issue,iso(today()))};const d=Math.max(0,diffDays(issue,iso(today())));if(d>=15)return {key:'bad',label:'Critical Delay',score:0,days:d};if(d>=11)return {key:'bad',label:'Delayed',score:1,days:d};if(d>=10)return {key:'warn',label:'Target Due',score:2,days:d};if(d>=7)return {key:'warn',label:'Follow-up Now',score:3,days:d};return {key:'good',label:'On Track',score:5,days:d}}
function pill(key,label){return `<span class="pill ${key}">${esc(label)}</span>`}
function progress(v){v=Math.max(0,Math.min(100,n(v)));return `<b>${v}%</b><div class="progress"><i style="width:${v}%"></i></div>`}
function makeModels(){const pmap=new Map(DATA.products.map(r=>[String((r.value||{}).code||r.key.split(':')[1]),r.value||{}]));const allQs=DATA.quotations.map(r=>r.value||{}).filter(q=>q.number);const firstByClient=new Map();for(const q of allQs){const c=norm(qClient(q)), d=qDate(q);if(!d)continue;if(!firstByClient.has(c)||d<firstByClient.get(c))firstByClient.set(c,d)}PROJECTS=allQs.map(q=>{const wf=DATA.workflows.get(q.number)||{};const client=qClient(q), qd=qDate(q);const pitems=(Array.isArray(q.items)?q.items:[]).filter(it=>{const p=pmap.get(String(it.code||''))||{};return String(p.category||'')==='Printed Cups'});const hasPrinted=pitems.length>0;const printedQty=pitems.reduce((s,it)=>s+n(it.qty),0);const printedNames=[...new Set(pitems.map(it=>String(it.description||it.name||'Printed Cup').trim()).filter(Boolean))];let stage=wf.stage||defaultStage(q);if(q.orderStatus==='Completed')stage='Completed';const pct=wf.progress!=null?n(wf.progress):stagePct(stage);let supplierStage=wf.supplierStage||(q.orderStatus==='Completed'?'Completed':hasPrinted?'Sent to Supplier':'Not Sent to Supplier');const supplierProgress=wf.supplierProgress!=null?n(wf.supplierProgress):supplierPct(supplierStage);const issue=wf.supplierIssueDate||qd||iso(today());const due=wf.supplierDueDate||addDays(issue,10);const risk=hasPrinted?supplierRisk(issue,supplierStage):null;const first=firstByClient.get(norm(client))||qd;const newClient=!!first&&diffDays(first,iso(today()))>=0&&diffDays(first,iso(today()))<=6;return {q,wf,client,qd,first,newClient,stage,pct,nextAction:wf.nextAction||defaultNext(stage),followUpDate:wf.followUpDate||'',notes:wf.notes||'',updatedAt:wf.updatedAt||'',hasPrinted,printedQty,printedNames,supplierStage,supplierProgress,supplierName:wf.supplierName||'',issue,due,risk}}).sort((a,b)=>(b.qd||'').localeCompare(a.qd||''))}
function needle(){return norm($('#search').value)}
function match(p){const x=needle();return !x||[p.client,p.q.number,p.stage,p.q.orderStatus,p.supplierName,p.printedNames.join(' ')].some(v=>norm(v).includes(x))}
function dueFollow(p){return p.followUpDate&&dparse(p.followUpDate)&&dparse(p.followUpDate)<=today()&&p.stage!=='Completed'}
function active(p){return p.q.orderStatus!=='Completed'}
function supplierOpen(p){return p.hasPrinted&&active(p)&&!['Received','Completed'].includes(p.supplierStage)}
function priority(p){if(dueFollow(p))return 0;if(p.risk&&p.risk.score<=1)return 1;if(p.risk&&p.risk.score<=3)return 2;if(p.newClient)return 3;return 5}
function renderOverview(){const ms=PROJECTS.filter(match);const uniqueNew=new Set(PROJECTS.filter(p=>p.newClient).map(p=>norm(p.client)));$('#kNew').textContent=uniqueNew.size;$('#kActive').textContent=PROJECTS.filter(active).length;$('#kFollow').textContent=PROJECTS.filter(dueFollow).length;$('#kSupplier').textContent=PROJECTS.filter(supplierOpen).length;$('#kRisk').textContent=PROJECTS.filter(p=>supplierOpen(p)&&p.risk&&p.risk.days>=7).length;const rows=ms.filter(active).sort((a,b)=>priority(a)-priority(b)||(a.due||'9999').localeCompare(b.due||'9999')).slice(0,40);$('#actionBody').innerHTML=rows.length?rows.map(p=>{const pri=dueFollow(p)?pill('bad','Follow-up Due'):p.risk&&p.risk.days>=10?pill('bad','Supplier Delay'):p.risk&&p.risk.days>=7?pill('warn','Supplier Follow-up'):p.newClient?pill('new','New Client'):pill('neutral','Active');return `<tr><td>${pri}</td><td><b>${esc(p.client)}</b>${p.newClient?'<div class="mini">New client</div>':''}</td><td>${esc(p.q.number)}</td><td>${esc(p.stage)}</td><td>${progress(p.pct)}</td><td>${esc(p.nextAction)}</td><td>${esc(p.followUpDate||'—')}</td><td>${p.risk?pill(p.risk.key,p.risk.label):'—'}</td><td><button class="btn edit" data-q="${esc(p.q.number)}">Update</button></td></tr>`}).join(''):'<tr><td colspan="9"><div class="empty">No active projects.</div></td></tr>'}
function renderClients(){const rows=PROJECTS.filter(p=>active(p)&&match(p));$('#clientBody').innerHTML=rows.length?rows.map(p=>`<tr><td>${esc(p.qd||'—')}</td><td><b>${esc(p.client)}</b>${p.newClient?'<div class="mini">New this week</div>':''}</td><td>${esc(p.q.number)}</td><td>${pill('neutral',p.q.orderStatus||'—')}</td><td>${esc(p.stage)}</td><td>${progress(p.pct)}</td><td>${esc(p.nextAction)}</td><td>${esc(p.followUpDate||'—')}</td><td>${esc(p.updatedAt?p.updatedAt.replace('T',' ').slice(0,16):'Auto')}</td><td><button class="btn edit" data-q="${esc(p.q.number)}">Update</button></td></tr>`).join(''):'<tr><td colspan="10"><div class="empty">No active client projects.</div></td></tr>'}
function renderSupplier(){const rows=PROJECTS.filter(p=>p.hasPrinted&&active(p)&&match(p)).sort((a,b)=>(a.risk?.score??9)-(b.risk?.score??9)||(a.due||'').localeCompare(b.due||''));$('#supplierBody').innerHTML=rows.length?rows.map(p=>`<tr><td>${esc(p.issue)}</td><td><b>${esc(p.due)}</b></td><td><b>${esc(p.client)}</b></td><td>${esc(p.q.number)}</td><td>${esc(p.printedNames.join(' / '))}</td><td class="num"><b>${fmt(p.printedQty)}</b></td><td>${esc(p.supplierStage)}</td><td>${progress(p.supplierProgress)}</td><td class="num">${p.risk?p.risk.days:0} d</td><td>${p.risk?pill(p.risk.key,p.risk.label):'—'}${p.risk&&p.risk.days>=7?'<div class="mini">Follow up supplier now</div>':''}</td><td>${esc(p.supplierName||'—')}</td><td><button class="btn edit" data-q="${esc(p.q.number)}">Update</button></td></tr>`).join(''):'<tr><td colspan="12"><div class="empty">No open printed-cup supplier jobs.</div></td></tr>'}
function renderCompleted(){const rows=PROJECTS.filter(p=>!active(p)&&match(p));$('#completedBody').innerHTML=rows.length?rows.map(p=>`<tr><td>${esc(p.qd||'—')}</td><td><b>${esc(p.client)}</b></td><td>${esc(p.q.number)}</td><td>${pill('good','Completed')}</td><td>${esc(p.stage)} · ${p.pct}%</td><td>${p.hasPrinted?esc(p.supplierStage):'—'}</td><td>${esc(p.updatedAt?p.updatedAt.replace('T',' ').slice(0,16):'Auto')}</td><td><button class="btn edit" data-q="${esc(p.q.number)}">View / Update</button></td></tr>`).join(''):'<tr><td colspan="8"><div class="empty">No completed projects match.</div></td></tr>'}
function bindEdits(){$$('.edit').forEach(b=>b.onclick=()=>openModal(b.dataset.q))}
function render(){renderOverview();renderClients();renderSupplier();renderCompleted();bindEdits()}
function fillSelect(el,items,val){el.innerHTML=items.map(x=>`<option ${x[0]===val?'selected':''}>${esc(x[0])}</option>`).join('')}
function openModal(qno){CURRENT=PROJECTS.find(p=>p.q.number===qno);if(!CURRENT)return;$('#modalTitle').textContent=`Update ${qno}`;$('#mClient').value=CURRENT.client;$('#mQuotation').value=qno;fillSelect($('#mStage'),CLIENT_STAGES,CURRENT.stage);$('#mProgress').value=CURRENT.pct;$('#mNext').value=CURRENT.nextAction;$('#mFollow').value=CURRENT.followUpDate||'';$('#mSupplier').value=CURRENT.supplierName||'';fillSelect($('#mSupplierStage'),SUPPLIER_STAGES,CURRENT.supplierStage);$('#mSupplierProgress').value=CURRENT.supplierProgress;$('#mIssue').value=CURRENT.issue||'';$('#mDue').value=CURRENT.due||'';$('#mNotes').value=CURRENT.notes||'';$('#modal').classList.remove('hidden')}
function closeModal(){CURRENT=null;$('#modal').classList.add('hidden')}
async function saveCurrent(){if(!CURRENT)return;const val={quotationNumber:CURRENT.q.number,client:CURRENT.client,stage:$('#mStage').value,progress:Math.max(0,Math.min(100,n($('#mProgress').value))),nextAction:$('#mNext').value.trim(),followUpDate:$('#mFollow').value||'',supplierName:$('#mSupplier').value.trim(),supplierStage:$('#mSupplierStage').value,supplierProgress:Math.max(0,Math.min(100,n($('#mSupplierProgress').value))),supplierIssueDate:$('#mIssue').value||CURRENT.issue,supplierDueDate:$('#mDue').value||addDays($('#mIssue').value||CURRENT.issue,10),notes:$('#mNotes').value.trim(),updatedAt:new Date().toISOString()};$('#save').disabled=true;$('#save').textContent='Saving…';const {error}=await sb.from('ecohub_kv').upsert({key:`workflow:${CURRENT.q.number}`,value:val},{onConflict:'key'});$('#save').disabled=false;$('#save').textContent='Save Update';if(error){alert('Could not save: '+error.message);return}closeModal();await load()}
async function load(){try{$('#status').textContent='Refreshing quotations and workflow data…';const [q,p,w]=await Promise.all([sb.from('ecohub_kv').select('key,value').like('key','quotation:%'),sb.from('ecohub_kv').select('key,value').like('key','product:%'),sb.from('ecohub_kv').select('key,value').like('key','workflow:%')]);if(q.error)throw q.error;if(p.error)throw p.error;if(w.error)throw w.error;DATA.quotations=q.data||[];DATA.products=p.data||[];DATA.workflows=new Map((w.data||[]).map(r=>[String((r.value||{}).quotationNumber||r.key.split(':').slice(1).join(':')),r.value||{}]));makeModels();render();$('#status').textContent=`Updated ${new Date().toLocaleString('en-PH')} · ${PROJECTS.length} quotations · ${PROJECTS.filter(p=>p.hasPrinted).length} printed-cup jobs`}catch(e){console.error(e);$('#status').textContent='Could not load control center: '+(e.message||e)}}
function show(session){$('#auth').classList.toggle('hidden',!!session);$('#app').classList.toggle('hidden',!session);if(session)load()}
$('#login').onclick=async()=>{const {error}=await sb.auth.signInWithPassword({email:$('#email').value.trim(),password:$('#pass').value});$('#authErr').textContent=error?error.message:''};$('#logout').onclick=async()=>{await sb.auth.signOut();show(null)};$('#refresh').onclick=load;$('#back').onclick=()=>location.href='./';$('#search').oninput=render;$('#cancel').onclick=closeModal;$('#save').onclick=saveCurrent;$('#mStage').onchange=()=>$('#mProgress').value=stagePct($('#mStage').value);$('#mSupplierStage').onchange=()=>$('#mSupplierProgress').value=supplierPct($('#mSupplierStage').value);$('#mIssue').onchange=()=>{if($('#mIssue').value)$('#mDue').value=addDays($('#mIssue').value,10)};$$('.nav button').forEach(b=>b.onclick=()=>{$$('.nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');$$('.view').forEach(x=>x.classList.remove('active'));$('#'+b.dataset.view).classList.add('active')});sb.auth.onAuthStateChange((_e,s)=>show(s));sb.auth.getSession().then(({data})=>show(data.session));
})();